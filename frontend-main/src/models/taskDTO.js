module.exports = function (category, title, tags, discription, solution, maxPoints) {
  this.category = category
  this.title = title
  this.tags = tags
  this.discription = discription
  this.solution = solution
  this.maxPoints = maxPoints
}
